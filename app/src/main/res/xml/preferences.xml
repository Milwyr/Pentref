<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="30"
        android:entries="@array/pref_notification_minutes_before_departure_options"
        android:entryValues="@array/pref_notification_minutes_before_departure_options"
        android:key="pref_key_notification_minutes_before_departure"
        android:summary="@string/pref_notification_summary"
        android:title="Scheduled notification for transportation" />

    <Preference
        android:key="synchronise_with_server"
        android:summary="@string/summary_synchronise_now"
        android:title="@string/synchronise_with_server" />
</PreferenceScreen>